# Managed by InfraBox Platform; authentication, ports and host keys are unchanged.
LogLevel {{ sshd_log_level }}
ClientAliveInterval {{ sshd_client_alive_interval }}
ClientAliveCountMax {{ sshd_client_alive_count_max }}
